# Qubit/bottleneck ablation (revision, R1 comment 3): vary n_qubits to justify
# the 8-qubit headline architecture. 8-qubit results already exist as
# baseline_C3_hybrid_breast. The classical bottleneck width equals n_qubits.
experiment_id: ablation_C3_hybrid_breast_q4
model:
  name: hybrid_qnn
  in_channels: 1
  num_classes: 2
  n_qubits: 4
  n_layers: 2
  noise_p: 0.0
  shots_per_inference: 1000
data:
  dataset: breastmnist
  batch_size: 16
  root: data/raw
training:
  epochs: 10
  lr: 5.0e-3
  class_weighting: true
  seed: [0, 1, 2]
# NISQ-relevant noise levels only — the full sweep lives in the baselines.
noise_sweep: [0.0, 1.0e-3, 1.0e-2]
